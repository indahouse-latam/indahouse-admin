# ===========================================
# Indahouse Admin – QA (rama develop)
# ===========================================
# Copia a .env.local para desarrollo local.
# En Vercel Preview (develop) configura estas variables en Environment Variables.
# Todo proveedor privado en NEXT_PUBLIC queda visible en el navegador; restringelo por dominio y cuota.

# Backend API (QA)
NEXT_PUBLIC_API_URL=https://api-qa.indahouse.com.co/api/v1

# App URL (WebAuthn origin + Google callback). Nyx QA must allowlist this origin
# (WEBAUTHN_ORIGIN / CORS / Google redirect). Parent RP_ID indahouse.com.co covers
# app-qa + admin-qa; otherwise Nyx must add this host explicitly.
NEXT_PUBLIC_APP_URL=https://admin-qa.indahouse.com.co

# Nyx Wallet V3 (server-only — do not use NEXT_PUBLIC_ for the API key)
# Must be the same Nyx host as indahouse-api NYX_API_BASE.
NYX_API_BASE=https://nyx-wallet-api-v3.ledgit.tech/api
NYX_V3_CLIENT_API_KEY=your-qa-nyx-v3-client-api-key

# Google Maps (autocomplete direcciones)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=your-google-maps-key

# Entorno: QA usa Polygon Amoy (testnet)
NEXT_PUBLIC_APP_ENV=qa
NEXT_PUBLIC_POLYGON_AMOY_RPC_URL=https://polygon-amoy.drpc.org
